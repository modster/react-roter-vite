import type { Config } from "@react-router/dev/config";

// Server side routing
export default {
  ssr: true,
} satisfies Config;

/* -------------------------------------------------------
// Client side routing
export default {
  ssr: false,
} satisfies Config; */
